# Core dependencies
pynput>=1.7.6
pandas>=2.0.0
numpy>=1.24.0
matplotlib>=3.8.0

# Windows API (RDP compatible automation)
pywin32>=306; sys_platform == 'win32'

# System tray
pystray>=0.19.0
pillow>=10.0.0

# Build dependencies
pyinstaller>=6.0.0
